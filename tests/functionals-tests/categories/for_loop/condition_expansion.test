foo="a b c             d"
bar="Goodbye World"

echo $foo $bar

#for i in $foo ${bar}; do
for i in $foo; do
    #echo "$i" '$i' "${i}";
    echo $i;
    #bar=$foo
    #foo=$bar$bar
done;

echo $foo $bar
